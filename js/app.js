/* ============================================================
   APP — boot sequence. Everything else is loaded and ready by
   the time this file runs (script order defined in index.html).
   ============================================================ */

initSupabase();
